kind: AppProject
apiVersion: argoproj.io/v1alpha1
metadata:
  name: "rhadp"
  namespace: "{{ gitops_namespace }}"
spec:
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  destinations:
    - namespace: "*"
      server: "*"
  sourceRepos:
    - "*"
